# Only needed for local development, when `next dev` runs on :3000 and the
# FastAPI backend runs on :8000. In production FastAPI serves the built
# frontend itself, so the API is same-origin and this stays empty.
NEXT_PUBLIC_API_BASE=http://localhost:8000
